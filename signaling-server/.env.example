# Railway sets PORT automatically
PORT=3001
HOST=0.0.0.0
NODE_ENV=production

# Production on Railway: always HTTP inside the container (edge = WSS)
SIGNALING_USE_HTTP=true

# Local dev with Frontend/certs: copy certs to signaling-server/certs/
# or set SIGNALING_USE_HTTP=true and use ws://
